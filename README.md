# remember_game2

1:1 ??? ??? ?? (Express + Socket.IO)

## ?? ?? ??

1. **??? ??**
   ```bash
   npm install
   ```

2. **??** (????? + ??)
   ```bash
   npm run build
   ```
   - ??????: `npm run build:client`
   - ???: `npm run build:server`

3. **?? ??**
   ```bash
   npm run server
   ```
   - ??? `http://localhost:3000` ?? ?????.

4. **?????? ??**
   - ???? **http://localhost:3000** ?? ? ??
   - ???(???) ? ???? ? ??? ?? ?? ? ??

?? PC?? ? ?? ????(?? ??? ?)? ?? ???? 1:1 ?? ??? ?????.

## GitHub Pages ?? (Vite)

1. **??? ?? ??**  
   `vite.config.ts`? `REPO_NAME`? GitHub ??? ??? ??? ??? (??: `remember_game2`).

2. **?? ? ??**
   ```bash
   npm run deploy
   ```
   - `vite build`? ????? ?? ? `gh-pages -d dist`? `gh-pages` ???? ??

3. **GitHub ??**  
   ??? ? Settings ? Pages ? Source: **Deploy from a branch** ? Branch: **gh-pages** / **(root)** ? Save

4. **??**  
   `https://<username>.github.io/remember_game2/` ?? ??

- ?? ????: `npm run dev:vite` (?? ??), `npm run preview` (?? ?? ????)

### ???? ?? ???? (??�??�???)

1. **??? ??**: `public/og-image.png` ? ?? ? ?? ???? ????. ?? ??: **1200�630** ??.
2. **URL ??**: `index.html` ?? `YOUR_USERNAME` ? ?? GitHub ?????? ????.  
   - `og:image`, `og:url`, `twitter:image` ? ? ?? ??.
3. ?? ? ????? ??? ????? ??�??�???? ????? ?????.

## ??

- **?? ? ?? ???**: `npm run dev` (nodemon)
- **PM2? ?? ??**: `npm run pm2:start`
